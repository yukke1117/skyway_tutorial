import { SignalingSession } from '../../../../external/signaling';
export declare class MessageBuffer {
    readonly signaling: SignalingSession;
    private _indicateMessageBuffer;
    private _excludeConnectionIndicateBuffering;
    private _disposer;
    constructor(signaling: SignalingSession);
    resolveMessagingBuffer({ id, name }: {
        id: string;
        name?: string;
    }): void;
    close(): void;
}
//# sourceMappingURL=messageBuffer.d.ts.map