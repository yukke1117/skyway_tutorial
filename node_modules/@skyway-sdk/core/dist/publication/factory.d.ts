import model from '@skyway-sdk/model';
import { SkyWayChannelImpl } from '../channel';
import { LocalStream } from '../media/stream/local';
import { PublicationImpl } from '.';
/**@internal */
export declare function createPublication<T extends LocalStream>(channel: SkyWayChannelImpl, { publisherId, stream, origin, metadata, codecCapabilities, encodings, contentType, id, isEnabled, }: model.Publication & {
    stream?: T;
}): PublicationImpl<T>;
//# sourceMappingURL=factory.d.ts.map