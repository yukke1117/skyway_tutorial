import { LocalPersonAdapter, LocalStream, RemoteAudioStream, RemoteDataStream, RemoteVideoStream } from '@skyway-sdk/core';
import { RoomPublication } from '../../publication';
import { P2PRoomImpl } from '../../room/p2p';
import { RoomSubscription } from '../../subscription';
import { LocalRoomMember, LocalRoomMemberImpl, RoomPublicationOptions } from './base';
export interface LocalP2PRoomMember extends LocalRoomMember {
    /**
     * @description [japanese] StreamをPublishする
     */
    publish: <T extends LocalStream = LocalStream>(stream: T, options?: RoomPublicationOptions) => Promise<RoomPublication<T>>;
}
/**@internal */
export declare class LocalP2PRoomMemberImpl extends LocalRoomMemberImpl implements LocalP2PRoomMember {
    /**@private */
    constructor(member: LocalPersonAdapter, room: P2PRoomImpl);
    publish<T extends LocalStream = LocalStream>(stream: LocalStream, options?: RoomPublicationOptions): Promise<RoomPublication<T>>;
    unpublish(target: string | RoomPublication): Promise<void>;
    subscribe<T extends RemoteVideoStream | RemoteAudioStream | RemoteDataStream>(target: string | RoomPublication): Promise<{
        subscription: RoomSubscription<T>;
        stream: T;
    }>;
    unsubscribe(target: string | RoomSubscription): Promise<void>;
    _updateRoom(room: P2PRoomImpl): void;
}
//# sourceMappingURL=p2p.d.ts.map